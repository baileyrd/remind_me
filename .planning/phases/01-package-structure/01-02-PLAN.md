---
phase: 01-package-structure
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - remind_me_mcp/importer.py
  - remind_me_mcp/pid.py
  - remind_me_mcp/server.py
  - remind_me_mcp/tools.py
  - remind_me_mcp/api.py
  - remind_me_mcp/dashboard/__init__.py
  - remind_me_mcp/dashboard/App.jsx
autonomous: true
requirements:
  - ARCH-01
  - ARCH-04
  - ARCH-05
  - QUAL-03

must_haves:
  truths:
    - "importer.py exports import_chat_file and all parser/chunker helpers"
    - "pid.py exports _read_pid_file, _write_pid_file, _remove_pid_file, _check_ui_server_health, get_server_status"
    - "server.py exports the FastMCP instance (mcp) and app_lifespan"
    - "tools.py registers all 13 MCP tools and 2 resources on the mcp instance from server.py"
    - "api.py exports _build_api_app and _build_dashboard_html where the HTML loads JSX from dashboard/App.jsx at runtime"
    - "Dashboard App.jsx file contains the full React component extracted from the Python string"
  artifacts:
    - path: "remind_me_mcp/importer.py"
      provides: "Chat import engine with all parsers and chunker"
      contains: "def import_chat_file"
    - path: "remind_me_mcp/pid.py"
      provides: "PID file management and server status"
      contains: "def get_server_status"
    - path: "remind_me_mcp/server.py"
      provides: "FastMCP instance and lifespan"
      contains: "mcp = FastMCP"
    - path: "remind_me_mcp/tools.py"
      provides: "All 13 MCP tool handlers and 2 resource handlers"
      contains: "@mcp.tool"
    - path: "remind_me_mcp/api.py"
      provides: "Starlette HTTP API app builder and dashboard HTML builder"
      contains: "_build_api_app"
    - path: "remind_me_mcp/dashboard/App.jsx"
      provides: "React dashboard component (no longer embedded in Python)"
      contains: "function App"
  key_links:
    - from: "remind_me_mcp/tools.py"
      to: "remind_me_mcp/server.py"
      via: "imports mcp instance for @mcp.tool decorators"
      pattern: "from remind_me_mcp\\.server import mcp"
    - from: "remind_me_mcp/tools.py"
      to: "remind_me_mcp/db.py"
      via: "imports _get_db, _embed_and_store, _semantic_search, etc."
      pattern: "from remind_me_mcp\\.db import"
    - from: "remind_me_mcp/api.py"
      to: "remind_me_mcp/db.py"
      via: "imports _get_db and helpers for REST handlers"
      pattern: "from remind_me_mcp\\.db import"
    - from: "remind_me_mcp/api.py"
      to: "remind_me_mcp/dashboard/App.jsx"
      via: "_build_dashboard_html reads JSX file from disk"
      pattern: "Path.*dashboard.*App\\.jsx"
    - from: "remind_me_mcp/importer.py"
      to: "remind_me_mcp/db.py"
      via: "imports _get_db, _make_id, _now_iso, _embed_and_store"
      pattern: "from remind_me_mcp\\.db import"
---

<objective>
Extract all behavioral modules from the monolith: chat importer, PID management, MCP server instance, tool handlers, HTTP API, and the dashboard JSX.

Purpose: Move the remaining ~1,900 lines of the monolith into their proper modules. After this plan, all code lives in `remind_me_mcp/` and the original `remind_me_mcp.py` is no longer needed.

Output: Six new modules plus a dashboard subdirectory with the extracted JSX file.
</objective>

<execution_context>
@/home/baileyrd/.claude/get-shit-done/workflows/execute-plan.md
@/home/baileyrd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/01-package-structure/01-01-SUMMARY.md
@remind_me_mcp.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create importer.py and pid.py</name>
  <files>
    remind_me_mcp/importer.py
    remind_me_mcp/pid.py
  </files>
  <action>
**importer.py** — Extract from `remind_me_mcp.py` lines 580-806 (the chat import engine):
- `import_chat_file(path, category, tags, extract_mode, max_length, db)` — the main import function
- `_chunk_text(text, max_length)` — text chunking helper
- `_extract_messages_from_json(data)` — JSON format parser
- `_filter_messages(messages, mode)` — message filtering by extract_mode
- `_parse_markdown_chat(text)` — Markdown format parser
- Import from package: `from remind_me_mcp.db import _get_db, _make_id, _now_iso, _embed_and_store, _row_to_dict`
- Standard lib imports: `hashlib`, `json`, `logging`, `re`, `pathlib.Path`
- Module logger: `log = logging.getLogger("remind_me_mcp.importer")`
- Define `__all__` listing `import_chat_file` and the private helpers (they are used by tools/api)

Note: `import_chat_file` currently calls `_get_db()` internally. Keep that pattern for now (Phase 3 will refactor to accept db as parameter per DATA-03). Extract the function signature exactly as-is.

**pid.py** — Extract from `remind_me_mcp.py` lines 53-119 (server instance detection):
- `_read_pid_file()` function
- `_write_pid_file(host, port)` function
- `_remove_pid_file()` function
- `_check_ui_server_health(url)` function
- `get_server_status()` function
- Import from package: `from remind_me_mcp.config import PID_FILE, DB_PATH`
- Standard lib imports: `json`, `logging`, `os`
- The `_now_iso` import from `.db` for `_write_pid_file` (it uses `_now_iso()`)
- Module logger: `log = logging.getLogger("remind_me_mcp.pid")`
- Define `__all__` listing all 5 functions

Preserve all existing function signatures, docstrings, and behavior exactly.
  </action>
  <verify>
    <automated>cd /home/baileyrd/projects/remind_me && python -c "from remind_me_mcp.importer import import_chat_file, _chunk_text, _extract_messages_from_json, _filter_messages, _parse_markdown_chat; print('importer OK')" && python -c "from remind_me_mcp.pid import _read_pid_file, _write_pid_file, _remove_pid_file, _check_ui_server_health, get_server_status; print('pid OK')"</automated>
  </verify>
  <done>importer.py exports import_chat_file and all parser helpers. pid.py exports all 5 PID/status functions. Both import without error.</done>
</task>

<task type="auto">
  <name>Task 2: Create server.py, tools.py, api.py, and extract dashboard JSX</name>
  <files>
    remind_me_mcp/server.py
    remind_me_mcp/tools.py
    remind_me_mcp/api.py
    remind_me_mcp/dashboard/__init__.py
    remind_me_mcp/dashboard/App.jsx
  </files>
  <action>
**server.py** — Extract from `remind_me_mcp.py` lines 808-823:
- `app_lifespan(app)` async context manager (imports `_get_db` from `.db`, `DB_PATH` from `.config`)
- `mcp = FastMCP("remind_me_mcp", lifespan=app_lifespan)` instance creation
- Import: `from contextlib import asynccontextmanager`, `from mcp.server.fastmcp import FastMCP`
- Import: `from remind_me_mcp.config import DB_PATH` and `from remind_me_mcp.db import _get_db`
- Module logger: `log = logging.getLogger("remind_me_mcp.server")`
- Define `__all__ = ["mcp", "app_lifespan"]`

**CRITICAL import order:** `server.py` must NOT import from `tools.py`. Instead, `tools.py` imports `mcp` from `server.py` and registers tools onto it. This avoids circular imports.

**tools.py** — Extract from `remind_me_mcp.py` lines 827-1599 (all 13 tool handlers + 2 resource handlers):
- All `@mcp.tool(...)` decorated async functions (13 total):
  - `memory_add`, `memory_search`, `memory_list`, `memory_get`, `memory_update`, `memory_delete`
  - `memory_import_chat`, `memory_import_directory`, `memory_stats`
  - `remind_me_auto_capture`, `remind_me_get_capture`, `remind_me_reindex`, `remind_me_server_status`
- All `@mcp.resource(...)` decorated async functions (2 total):
  - `resource_stats`, `resource_categories`
- Import `mcp` from `.server`
- Import all needed helpers from `.db`: `_get_db`, `_embed_and_store`, `_semantic_search`, `_now_iso`, `_make_id`, `_row_to_dict`
- Import all Pydantic models from `.models`
- Import `_fmt_memory_md`, `_fmt_memories` from `.formatting`
- Import `import_chat_file` from `.importer`
- Import `get_server_status` from `.pid`
- Standard lib imports: `json`, `logging`, `pathlib.Path`
- Module logger: `log = logging.getLogger("remind_me_mcp.tools")`
- Define `__all__` listing all 15 handler function names

**api.py** — Extract from `remind_me_mcp.py` lines 1603-1871 and 1874-2433:
- `_build_api_app()` function (the Starlette app builder with all route handlers defined inside it)
- `_build_dashboard_html()` function — **MODIFY** this to read `App.jsx` from the `dashboard/` subdirectory instead of returning the inline Python string. The HTML template stays in Python (it's the wrapper), but the JSX script content comes from reading `dashboard/App.jsx` at startup.
- The HTML wrapper should use `importlib.resources` or `pathlib.Path(__file__).parent / "dashboard" / "App.jsx"` to locate the JSX file. Use the `Path` approach (simpler, no version compatibility issues).
- Keep all Starlette imports lazy (inside `_build_api_app`) as they currently are.
- Import from package: `from remind_me_mcp.config import DB_PATH`, `from remind_me_mcp.db import _get_db, _row_to_dict, _embed_and_store, _now_iso, _make_id`, `from remind_me_mcp.importer import import_chat_file`
- Module logger: `log = logging.getLogger("remind_me_mcp.api")`
- Define `__all__ = ["_build_api_app", "_build_dashboard_html"]`

**dashboard/__init__.py** — Empty file (makes it a subpackage).

**dashboard/App.jsx** — Extract the JSX from the Python string in `_get_dashboard_script()` (lines 1895-2433 approximately). This is the content between the triple-quoted string delimiters. Copy it verbatim — the exact JavaScript/JSX code that was inside the Python string. Do NOT use the `remind_me_dashboard.jsx` reference file at root (it uses ES module imports which Babel standalone cannot handle). Extract the **inline version** from `_get_dashboard_script()` which uses `React.createElement` patterns compatible with Babel standalone and CDN-loaded React.

Preserve all existing function signatures, route paths, and API behavior exactly.
  </action>
  <verify>
    <automated>cd /home/baileyrd/projects/remind_me && python -c "from remind_me_mcp.server import mcp, app_lifespan; print('server OK')" && python -c "from remind_me_mcp.tools import memory_add, memory_search; print('tools OK (sample)')" && python -c "from remind_me_mcp.api import _build_api_app, _build_dashboard_html; print('api OK')" && python -c "from pathlib import Path; jsx = Path('remind_me_mcp/dashboard/App.jsx'); assert jsx.exists(), 'App.jsx missing'; assert len(jsx.read_text()) > 1000, 'App.jsx too small'; print('dashboard JSX OK')"</automated>
  </verify>
  <done>server.py exports mcp instance and app_lifespan. tools.py registers all 13 tools and 2 resources. api.py exports _build_api_app and _build_dashboard_html (reading JSX from file). dashboard/App.jsx contains the extracted React component. All imports resolve without circular dependency errors.</done>
</task>

</tasks>

<verification>
1. `python -c "from remind_me_mcp.server import mcp; print(type(mcp))"` — FastMCP instance exists
2. `python -c "from remind_me_mcp import tools; print('tools registered')"` — tools module loads (registering all decorators)
3. `python -c "from remind_me_mcp.api import _build_api_app"` — API builder loads
4. `test -f remind_me_mcp/dashboard/App.jsx` — JSX file extracted
5. `python -c "import remind_me_mcp.config; import remind_me_mcp.embeddings; import remind_me_mcp.db; import remind_me_mcp.models; import remind_me_mcp.formatting; import remind_me_mcp.importer; import remind_me_mcp.pid; import remind_me_mcp.server; import remind_me_mcp.tools; import remind_me_mcp.api; print('all modules import cleanly')"` — no circular imports
</verification>

<success_criteria>
- All 13 MCP tool handlers are in tools.py and decorated on the mcp instance from server.py
- Both MCP resources are in tools.py
- api.py builds the Starlette app with all existing routes
- _build_dashboard_html reads App.jsx from the dashboard/ subdirectory
- dashboard/App.jsx contains the full React component code
- No circular imports across all modules
- Each module has `__all__` and its own logger
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-structure/01-02-SUMMARY.md`
</output>
