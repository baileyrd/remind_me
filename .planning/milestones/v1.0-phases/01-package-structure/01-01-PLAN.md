---
phase: 01-package-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - remind_me_mcp/config.py
  - remind_me_mcp/models.py
  - remind_me_mcp/formatting.py
  - remind_me_mcp/db.py
  - remind_me_mcp/embeddings.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-04
  - ARCH-05

must_haves:
  truths:
    - "config.py exports MEMORY_DIR, DB_PATH, IMPORT_LOG, PID_FILE, EMBEDDING_MODEL, EMBEDDING_DIM, MODEL_DIR, SERVE_UI, UI_PORT constants"
    - "models.py exports all 9 Pydantic input models and ResponseFormat enum"
    - "formatting.py exports _fmt_memory_md and _fmt_memories functions"
    - "db.py exports _get_db, _ensure_schema, _embed_and_store, _semantic_search, _now_iso, _make_id, _row_to_dict"
    - "embeddings.py exports _Embedder class and _get_embedder factory"
    - "Each module has its own logger via logging.getLogger('remind_me_mcp.<module>')"
    - "Each module defines __all__"
  artifacts:
    - path: "remind_me_mcp/config.py"
      provides: "All module-level constants and environment configuration"
      contains: "MEMORY_DIR"
    - path: "remind_me_mcp/models.py"
      provides: "All Pydantic input models and ResponseFormat enum"
      contains: "class MemoryAddInput"
    - path: "remind_me_mcp/formatting.py"
      provides: "Memory formatting helpers"
      contains: "_fmt_memory_md"
    - path: "remind_me_mcp/db.py"
      provides: "Database connection, schema, helpers"
      contains: "_get_db"
    - path: "remind_me_mcp/embeddings.py"
      provides: "ONNX embedding engine"
      contains: "class _Embedder"
  key_links:
    - from: "remind_me_mcp/db.py"
      to: "remind_me_mcp/config.py"
      via: "imports DB_PATH, EMBEDDING_DIM"
      pattern: "from remind_me_mcp\\.config import"
    - from: "remind_me_mcp/db.py"
      to: "remind_me_mcp/embeddings.py"
      via: "imports _get_embedder for _embed_and_store and _semantic_search"
      pattern: "from remind_me_mcp\\.embeddings import"
    - from: "remind_me_mcp/embeddings.py"
      to: "remind_me_mcp/config.py"
      via: "imports EMBEDDING_MODEL, EMBEDDING_DIM, MODEL_DIR"
      pattern: "from remind_me_mcp\\.config import"
    - from: "remind_me_mcp/formatting.py"
      to: "remind_me_mcp/models.py"
      via: "imports ResponseFormat enum"
      pattern: "from remind_me_mcp\\.models import"
---

<objective>
Create the foundation modules of the `remind_me_mcp/` package by extracting configuration, Pydantic models, formatting helpers, database layer, and embedding engine from the monolith.

Purpose: Establish the leaf-level modules that all higher-level modules (tools, api, importer) will import from. Getting these right first prevents cascading refactoring later.

Output: Five Python modules in `remind_me_mcp/` that contain all the data-layer and utility code currently in `remind_me_mcp.py`.
</objective>

<execution_context>
@/home/baileyrd/.claude/get-shit-done/workflows/execute-plan.md
@/home/baileyrd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@remind_me_mcp.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.py, models.py, and formatting.py</name>
  <files>
    remind_me_mcp/config.py
    remind_me_mcp/models.py
    remind_me_mcp/formatting.py
  </files>
  <action>
Create the `remind_me_mcp/` directory.

**config.py** — Extract from `remind_me_mcp.py` lines 1-48:
- `from __future__ import annotations` at top
- All imports needed: `os`, `logging`, `sys`, `pathlib.Path`
- All module-level constants: `MEMORY_DIR`, `DB_PATH`, `IMPORT_LOG`, `PID_FILE`, `EMBEDDING_MODEL`, `EMBEDDING_DIM`, `MODEL_DIR`
- Also extract `SERVE_UI` and `UI_PORT` from line 1606-1607 (these are configuration, not API logic)
- The `MEMORY_DIR.mkdir(parents=True, exist_ok=True)` call
- The `logging.basicConfig(...)` call and module logger: `log = logging.getLogger("remind_me_mcp.config")`
- Define `__all__` listing all exported constants

**models.py** — Extract from `remind_me_mcp.py` lines 383-542:
- All Pydantic imports: `BaseModel`, `ConfigDict`, `Field`, `field_validator`
- `ResponseFormat` enum (from line 384-386)
- All 9 Pydantic input model classes: `MemoryAddInput`, `MemorySearchInput`, `MemoryListInput`, `MemoryUpdateInput`, `MemoryDeleteInput`, `ChatImportInput`, `MemoryStatsInput`, `BulkImportDirInput`, `AutoCaptureInput`
- Module logger: `log = logging.getLogger("remind_me_mcp.models")`
- Define `__all__` listing ResponseFormat and all 9 model classes
- Imports needed: `from __future__ import annotations`, `logging`, `pathlib.Path`, `enum.Enum`, `typing.Any`, `typing.Optional`

**formatting.py** — Extract from `remind_me_mcp.py` lines 547-578:
- `_fmt_memory_md(m: dict) -> str` function
- `_fmt_memories(memories: list[dict], fmt: ResponseFormat, total: int | None = None) -> str` function
- Import `ResponseFormat` from `.models`
- Import `json` for JSON serialization in `_fmt_memories`
- Module logger: `log = logging.getLogger("remind_me_mcp.formatting")`
- Define `__all__` with the two formatting functions

Preserve all existing function signatures, docstrings, and behavior exactly. Do NOT modify any logic — this is a pure extraction.
  </action>
  <verify>
    <automated>cd /home/baileyrd/projects/remind_me && python -c "from remind_me_mcp.config import MEMORY_DIR, DB_PATH, IMPORT_LOG, PID_FILE, EMBEDDING_MODEL, EMBEDDING_DIM, MODEL_DIR, SERVE_UI, UI_PORT; print('config OK')" && python -c "from remind_me_mcp.models import ResponseFormat, MemoryAddInput, MemorySearchInput, MemoryListInput, MemoryUpdateInput, MemoryDeleteInput, ChatImportInput, MemoryStatsInput, BulkImportDirInput, AutoCaptureInput; print('models OK')" && python -c "from remind_me_mcp.formatting import _fmt_memory_md, _fmt_memories; print('formatting OK')"</automated>
  </verify>
  <done>config.py exports all 9 constants, models.py exports ResponseFormat plus all 9 Pydantic models, formatting.py exports both formatting functions. All imports resolve without error.</done>
</task>

<task type="auto">
  <name>Task 2: Create db.py and embeddings.py</name>
  <files>
    remind_me_mcp/db.py
    remind_me_mcp/embeddings.py
  </files>
  <action>
**embeddings.py** — Extract from `remind_me_mcp.py` lines 125-231:
- `_Embedder` class with all methods: `__init__`, `_ensure_loaded`, `embed`, `embed_one`, `available` property
- `_get_embedder()` factory function with global `_embedder` singleton
- Import `EMBEDDING_MODEL`, `EMBEDDING_DIM`, `MODEL_DIR` from `.config`
- Import `numpy as np` at top level (it's a required dependency)
- Lazy imports inside `_ensure_loaded`: `huggingface_hub`, `tokenizers`, `onnxruntime` (keep existing pattern)
- Module logger: `log = logging.getLogger("remind_me_mcp.embeddings")`
- Define `__all__` with `_Embedder` and `_get_embedder`

**db.py** — Extract from `remind_me_mcp.py` lines 233-378:
- `_get_db()` function
- `_ensure_schema()` function
- `_embed_and_store()` function
- `_semantic_search()` function
- `_now_iso()` function
- `_make_id()` function
- `_row_to_dict()` function
- Import from `.config`: `DB_PATH`, `EMBEDDING_DIM`
- Import from `.embeddings`: `_get_embedder`
- Standard lib imports: `sqlite3`, `hashlib`, `json`, `struct` (struct is used in sqlite-vec operations)
- `from datetime import datetime, timezone`
- Module logger: `log = logging.getLogger("remind_me_mcp.db")`
- Define `__all__` listing all 7 exported functions

**Import dependency order** (NO circular imports):
- `config.py` → imports nothing from package
- `embeddings.py` → imports from `config`
- `db.py` → imports from `config` and `embeddings`

Preserve all existing function signatures, docstrings, and behavior exactly. Do NOT modify any logic.
  </action>
  <verify>
    <automated>cd /home/baileyrd/projects/remind_me && python -c "from remind_me_mcp.embeddings import _Embedder, _get_embedder; print('embeddings OK')" && python -c "from remind_me_mcp.db import _get_db, _ensure_schema, _embed_and_store, _semantic_search, _now_iso, _make_id, _row_to_dict; print('db OK')" && python -c "import remind_me_mcp.config; import remind_me_mcp.embeddings; import remind_me_mcp.db; import remind_me_mcp.models; import remind_me_mcp.formatting; print('all 5 modules import cleanly')"</automated>
  </verify>
  <done>embeddings.py exports _Embedder and _get_embedder. db.py exports all 7 database helper functions. All 5 foundation modules import without circular dependency errors.</done>
</task>

</tasks>

<verification>
1. `python -c "from remind_me_mcp.config import MEMORY_DIR, DB_PATH"` — config loads
2. `python -c "from remind_me_mcp.models import MemoryAddInput"` — models load
3. `python -c "from remind_me_mcp.formatting import _fmt_memory_md"` — formatting loads
4. `python -c "from remind_me_mcp.embeddings import _Embedder"` — embeddings load
5. `python -c "from remind_me_mcp.db import _get_db"` — db loads
6. All 5 modules import in a single Python process without circular import errors
7. Each module has a `__all__` attribute: `python -c "import remind_me_mcp.config; print(remind_me_mcp.config.__all__)"`
</verification>

<success_criteria>
- Five `.py` files exist in `remind_me_mcp/` directory
- Every constant, model class, and helper function from lines 1-578 of the monolith is extractable from the new modules
- No circular imports: importing all 5 modules in any order succeeds
- Each module has `__all__` defined and its own logger via `logging.getLogger("remind_me_mcp.<module>")`
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-structure/01-01-SUMMARY.md`
</output>
