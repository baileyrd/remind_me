---
phase: 11-decay-vitality-classification
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - remind_me_mcp/retrieval.py
  - remind_me_mcp/models.py
  - remind_me_mcp/tools.py
  - tests/test_retrieval.py
  - tests/test_tools.py
autonomous: true
requirements: [DECAY-03, DECAY-04, DECAY-05, CLSF-05]

must_haves:
  truths:
    - "Memories with vitality < 0.05 are excluded from default search results"
    - "Search accepts include_dormant=True to include dormant memories"
    - "Search accepts min_vitality parameter to filter by minimum vitality"
    - "Vitality is a fourth RRF signal in search ranking (higher vitality = better rank)"
    - "remind_me_vitality_report returns dormant count, active count, vault health metrics, and decay distribution"
  artifacts:
    - path: "remind_me_mcp/retrieval.py"
      provides: "rank_rrf with 4th vitality signal"
      contains: "vitality_rank"
    - path: "remind_me_mcp/models.py"
      provides: "include_dormant and min_vitality fields on MemorySearchInput"
      contains: "include_dormant"
    - path: "remind_me_mcp/tools.py"
      provides: "remind_me_vitality_report tool, dormant filtering in memory_search, record_access calls"
      contains: "remind_me_vitality_report"
    - path: "tests/test_retrieval.py"
      provides: "Tests for 4-signal RRF"
    - path: "tests/test_tools.py"
      provides: "Tests for dormant exclusion, vitality report"
  key_links:
    - from: "remind_me_mcp/tools.py"
      to: "remind_me_mcp/vitality.py"
      via: "record_access called on search results, is_dormant for filtering"
      pattern: "from remind_me_mcp\\.vitality import"
    - from: "remind_me_mcp/retrieval.py"
      to: "vitality field on memory dicts"
      via: "4th RRF signal sorted by vitality DESC"
      pattern: "vitality_rank"
    - from: "remind_me_mcp/tools.py"
      to: "remind_me_mcp/retrieval.py"
      via: "rank_rrf with vitality_results parameter"
      pattern: "rank_rrf"
---

<objective>
Wire vitality into the search pipeline (dormant exclusion, 4th RRF signal, search params) and add the vitality_report tool.

Purpose: Makes vitality operational -- dormant memories fade from search, vital memories rank higher, and users can monitor vault health.
Output: Updated retrieval.py with 4-signal RRF, updated search tool with dormant filtering, new vitality_report tool, comprehensive tests.
</objective>

<execution_context>
@/home/baileyrd/.claude/get-shit-done/workflows/execute-plan.md
@/home/baileyrd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-11/11-01-SUMMARY.md

@remind_me_mcp/retrieval.py
@remind_me_mcp/tools.py
@remind_me_mcp/models.py

<interfaces>
<!-- From Plan 01 (vitality.py) -->
```python
compute_vitality(base_weight, access_count, decay_rate, days_since_last_access) -> float
record_access(memory_id: str) -> float | None
is_dormant(vitality: float) -> bool
VITALITY_FLOOR = 0.05
```

<!-- Current rank_rrf signature -->
```python
def rank_rrf(
    keyword_results: list[dict],
    semantic_results: list[dict],
    *,
    k: int | None = None,
) -> list[dict]:
```

<!-- Current MemorySearchInput -->
```python
class MemorySearchInput(BaseModel):
    query: str
    category: str | None = None
    tags: list[str] | None = None
    limit: int = 20
    token_budget: int = 800
    response_format: ResponseFormat = ResponseFormat.MARKDOWN
```

<!-- Memory dict now has these fields from migration -->
accessed_at, access_count, decay_rate, vitality, base_weight, status, memory_type
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add vitality as 4th RRF signal and dormant filtering in search</name>
  <files>remind_me_mcp/retrieval.py, remind_me_mcp/models.py, remind_me_mcp/tools.py, tests/test_retrieval.py, tests/test_tools.py</files>
  <behavior>
    - Test 1: rank_rrf with vitality data produces _vitality_rank on each result
    - Test 2: Higher vitality memories get better (lower) vitality_rank
    - Test 3: RRF score now sums 4 reciprocal ranks (keyword, semantic, recency, vitality)
    - Test 4: Dormant memories (status='dormant') excluded from search by default
    - Test 5: include_dormant=True includes dormant memories in search results
    - Test 6: min_vitality=0.5 excludes memories with vitality below 0.5
    - Test 7: record_access is called for returned search results (access_count incremented)
  </behavior>
  <action>
**In retrieval.py**, update rank_rrf:
1. Add a fourth ranking signal: vitality_rank. Sort all unique memories by their "vitality" field DESC (higher vitality = rank 1). Memories without a "vitality" key default to 1.0 (backwards compatible).
2. Add _vitality_rank to each result dict.
3. RRF score becomes: sum of 1/(k+rank) for keyword, semantic, recency, AND vitality.
4. Update docstring to reflect 4 signals.

**In models.py**, update MemorySearchInput:
1. Add include_dormant: bool = Field(default=False, description="Include dormant memories (vitality < 0.05) in results")
2. Add min_vitality: float = Field(default=0.0, ge=0.0, le=1.0, description="Minimum vitality score filter. 0.0 means no filter (except dormant exclusion).")

**In tools.py**, update memory_search:
1. Import record_access, is_dormant from remind_me_mcp.vitality
2. After fetching FTS and semantic results, filter out dormant memories BEFORE RRF ranking (unless include_dormant=True):
   - If not params.include_dormant: filter out memories where status == 'dormant'
3. After dormant filter, if min_vitality > 0: filter out memories where vitality < min_vitality
4. After building final result list (post token budget), call record_access for each returned memory ID in a background task (asyncio.to_thread or gather). Do NOT block the response on this -- use fire-and-forget pattern with asyncio.create_task.

**In tests/test_retrieval.py**: Add tests for 4-signal RRF. Create test memories with "vitality" field set to different values, verify _vitality_rank is assigned and affects scoring.

**In tests/test_tools.py**: Add tests for dormant exclusion and include_dormant. Create test memories with status='dormant', verify they are excluded by default and included with include_dormant=True.
  </action>
  <verify>
    <automated>python -m pytest tests/test_retrieval.py tests/test_tools.py -x -v -k "vitality or dormant" && ruff check remind_me_mcp/retrieval.py remind_me_mcp/tools.py remind_me_mcp/models.py</automated>
  </verify>
  <done>RRF uses 4 signals including vitality. Dormant memories excluded by default. include_dormant and min_vitality params work. record_access called for returned results. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitality report tool</name>
  <files>remind_me_mcp/tools.py, remind_me_mcp/models.py, tests/test_tools.py</files>
  <behavior>
    - Test 1: remind_me_vitality_report returns total_memories, active_count, dormant_count
    - Test 2: Report includes average_vitality across all memories
    - Test 3: Report includes decay_distribution (count per memory_type)
    - Test 4: Report includes vitality_buckets (count of memories in ranges: 0-0.05, 0.05-0.25, 0.25-0.5, 0.5-0.75, 0.75-1.0)
    - Test 5: Report includes vault_health_score (active_count / total as percentage)
  </behavior>
  <action>
**In models.py**, add:
- VitalityReportInput with response_format: ResponseFormat = Field(default=ResponseFormat.JSON) and model_config extra="forbid"
- Add to __all__

**In tools.py**, add remind_me_vitality_report tool:
- name="remind_me_vitality_report", annotations: readOnlyHint=True
- Queries:
  - SELECT COUNT(*) FROM memories (total)
  - SELECT COUNT(*) FROM memories WHERE status='active' (active)
  - SELECT COUNT(*) FROM memories WHERE status='dormant' (dormant)
  - SELECT AVG(vitality) FROM memories (avg_vitality)
  - SELECT memory_type, COUNT(*) FROM memories GROUP BY memory_type (decay_distribution)
  - Vitality buckets: 5 COUNT queries with vitality range conditions OR a single query with CASE WHEN
- Build response dict:
  ```json
  {
    "total_memories": N,
    "active_count": N,
    "dormant_count": N,
    "average_vitality": 0.XX,
    "vault_health_score": "XX%",
    "decay_distribution": {"decision": N, "fact": N, ...},
    "vitality_buckets": {"0.00-0.05": N, "0.05-0.25": N, "0.25-0.50": N, "0.50-0.75": N, "0.75-1.00": N}
  }
  ```
- If response_format is markdown, format as readable report. If JSON, return json.dumps.

**In tests/test_tools.py**: Create a few test memories with different vitality/status/memory_type values, call the tool, verify all fields present with correct values.
  </action>
  <verify>
    <automated>python -m pytest tests/test_tools.py -x -v -k "vitality_report" && ruff check remind_me_mcp/tools.py remind_me_mcp/models.py</automated>
  </verify>
  <done>remind_me_vitality_report surfaces dormant count, vault health, decay distribution, and vitality buckets. Registered as MCP tool. All tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_retrieval.py tests/test_tools.py -x -v` -- all tests pass (no regressions)
- `python -m pytest tests/ -x --timeout=60` -- full suite passes
- `ruff check remind_me_mcp/` -- no lint warnings
- `python -c "from remind_me_mcp.retrieval import rank_rrf; help(rank_rrf)"` -- docstring mentions 4 signals
</verification>

<success_criteria>
- Vitality is a 4th RRF signal in search ranking
- Dormant memories excluded from default search, retrievable with include_dormant=True
- min_vitality parameter filters by vitality threshold
- record_access called for returned search results (fire-and-forget)
- remind_me_vitality_report returns comprehensive vault health metrics
- All tests pass, ruff clean, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/phase-11/11-03-SUMMARY.md`
</output>
